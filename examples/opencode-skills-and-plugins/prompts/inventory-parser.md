# Task: Implement The Inventory CSV Parser

Implement `parseInventoryCsv(csv)` in `src/inventory.js`.

The visible tests show the basic shape. Keep the public export unchanged and make the parser suitable for a small production import path rather than only the visible examples.

Prefer a small implementation with clear validation errors.
