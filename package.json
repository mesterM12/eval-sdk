{
  "name": "coding-agent-eval-cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "coding-agent-eval": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && vitest run test"
  },
  "dependencies": {
    "@ai-hero/sandcastle": "^0.7.0",
    "commander": "^15.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "execa": "^9.6.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
